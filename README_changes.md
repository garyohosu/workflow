# 変更メモ

この ZIP には、公開中の `worklog-pwa1` の `WORKFLOW.md` をベースに、
今回の会話で整理した以下を反映した完成版 `WORKFLOW.md` を含めています。

反映内容:
- Claude Code → Codex CLI 連携可否の実行モード判定
- 連携レビュー方式 / 単独レビュー方式の明文化
- 単一AIによる複数視点レビューの正式定義
- Reviewer A/B/C/D + Arbiter の観点整理
- 指摘の採否判定工程の追加
- 連携不能時の正常フォールバック規定
- `state/state.json` への `review_execution_mode` 追加
- ログ出力項目への実行モード追加

注意:
- 元リポジトリの公開版を元に再構成した完成版です。
- 実運用前に、既存の自動処理コードが参照している見出し名・セクション番号との整合だけは確認してください。
