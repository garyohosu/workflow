USECASE.md

買い物思い出しメモ PWA — ユースケース一覧

1. アクター

・エンドユーザー: アプリを利用する個人ユーザー（認証なし）

2. ユースケース一覧

UC-01 買い物項目を追加する
UC-02 未購入品一覧を確認する
UC-03 項目を購入済みにする
UC-04 購入済みを解除して未購入に戻す
UC-05 購入済み品を追加表示する
UC-06 データを端末に保存・復元する
UC-07 アプリをホーム画面に追加する（PWA）

3. ユースケース詳細

────────────────────────────────────────
UC-01 買い物項目を追加する
────────────────────────────────────────

主アクター: エンドユーザー
事前条件: アプリが開いている
事後条件: 入力した項目が未購入品リストの先頭に追加される

基本フロー:
1. ユーザーが入力欄に項目名を入力する
2. ユーザーが登録ボタンを押す（または Enter キーを押す）
3. 入力欄が空になる
4. 項目が未購入品リストの先頭に表示される
5. データがローカルストレージに保存される

代替フロー:
A1. 入力欄が空または空白のみの場合
   → 登録されない。入力欄はそのまま残る。
A2. 入力が 100 文字を超える場合
   → 登録されない。エラー状態を示す（実装依存）。

────────────────────────────────────────
UC-02 未購入品一覧を確認する
────────────────────────────────────────

主アクター: エンドユーザー
事前条件: アプリが開いている
事後条件: なし（参照のみ）

基本フロー:
1. アプリを開く
2. 未購入品リストが表示される（新しい順）
3. 各項目に購入チェックボタンと項目名が表示される

────────────────────────────────────────
UC-03 項目を購入済みにする
────────────────────────────────────────

主アクター: エンドユーザー
事前条件: 未購入品が1件以上存在する
事後条件: 操作した項目が購入済み品リストへ移動する

基本フロー:
1. ユーザーが未購入品の購入チェックボタンを押す
2. 項目が未購入品リストから消える
3. 項目が取り消し線付きで購入済み品リストの末尾に追加される
4. checkedAt に購入日時が記録される
5. データがローカルストレージに保存される

────────────────────────────────────────
UC-04 購入済みを解除して未購入に戻す
────────────────────────────────────────

主アクター: エンドユーザー
事前条件: 購入済み品が1件以上存在する
事後条件: 操作した項目が未購入品リストの先頭に移動する

基本フロー:
1. ユーザーが購入済み品のチェックを外す
2. 項目が購入済み品リストから消える
3. 取り消し線が解除される
4. 項目が未購入品リストの先頭に表示される
5. checkedAt が null にリセットされる
6. データがローカルストレージに保存される

────────────────────────────────────────
UC-05 購入済み品を追加表示する
────────────────────────────────────────

主アクター: エンドユーザー
事前条件: 購入済み品が 21 件以上存在する
事後条件: 非表示だった購入済み品が表示される

基本フロー:
1. 購入済み品リストに最大 20 件が表示されている
2. 「・・・」ボタンが表示されている
3. ユーザーが「・・・」ボタンを押す
4. 残りの購入済み品がすべて表示される
5. 「・・・」ボタンが消える

代替フロー:
A1. 購入済み品が 20 件以下の場合
   → 「・・・」ボタンは表示されない。

────────────────────────────────────────
UC-06 データを端末に保存・復元する
────────────────────────────────────────

主アクター: エンドユーザー（暗黙）
事前条件: 項目が1件以上登録されている
事後条件: ページ再読み込み後もデータが保持される

基本フロー:
1. ユーザーが項目を登録・更新する
2. データがローカルストレージに自動保存される
3. ユーザーがページを再読み込みまたはアプリを再起動する
4. ローカルストレージからデータを読み込み、一覧が復元される

代替フロー:
A1. ローカルストレージの読み書きに失敗した場合
   → 画面全体は停止しない。エラーメッセージを表示してよい。
A2. 保存データが破損していた場合
   → データを初期化して再利用可能とする。

────────────────────────────────────────
UC-07 アプリをホーム画面に追加する（PWA）
────────────────────────────────────────

主アクター: エンドユーザー
事前条件: HTTPS または localhost で動作している
事後条件: アプリがスマホのホーム画面に追加される

基本フロー:
1. ユーザーがブラウザのメニューから「ホーム画面に追加」を選択する
2. manifest の情報に基づきアイコン・アプリ名が設定される
3. ホーム画面にアイコンが追加される
4. アイコンからアプリを起動できる

補足:
・service worker が登録されているため、最低限のオフライン動作も可能。

4. ユースケースと受け入れ条件の対応

| UC | 対応する AC |
|----|-------------|
| UC-01 | AC-01, AC-02, AC-03, AC-04 |
| UC-02 | AC-04, AC-05 |
| UC-03 | AC-06 |
| UC-04 | AC-07 |
| UC-05 | AC-08, AC-09, AC-10 |
| UC-06 | AC-11, AC-12 |
| UC-07 | AC-13, AC-14 |
